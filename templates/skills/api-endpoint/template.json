{
  "name": "api-endpoint",
  "description": "Create REST API endpoints with route-controller-service pattern",
  "type": "skill",
  "variables": [
    {
      "name": "skill_name",
      "prompt": "Skill name?",
      "default": "api-endpoint",
      "required": true
    }
  ]
}
