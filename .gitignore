# Secrets & credentials
resources.md
.claude/settings.local.json

# Browser session data
chrome-profile/

# Subprojects (có git repo riêng)
backend/
frontend/
storefront/

# Monorepo override: fit-confidence-workspace gộp code subproject vào repo này
# (user decision) — re-include để commit, KHÔNG ignore như pattern trên.
!fit-confidence-workspace/backend/
!fit-confidence-workspace/frontend/
!fit-confidence-workspace/storefront/

# Harness + codegraph artifacts (local, không commit vào boilerplate)
.claude/state/
.codegraph/
state.db

# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Build output
dist/
build/
.cache/

# Environment
.env
.env.*

# Workspaces (init-workspace tạo bên trong boilerplate, mỗi cái có git riêng)
# init-workspace tự append <workspace-name>/ vào đây khi tạo workspace mới.
ordertracking-workspace-harness/

# Lessons (local only, không push lên github)
docs/lessions/
