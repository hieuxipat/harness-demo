# Feature Registry — Source of truth cho tất cả features trong workspace
# File này được cập nhật tự động bởi các skills: break-task, implement, check-quality
# Không chỉnh sửa thủ công trừ khi cần thiết

registry_version: "1.0.0"
workspace: ""                            # Tên workspace (set bởi init-workspace)
last_updated: null

features:
  # --- EXAMPLE --- Xóa entry này khi tạo feature thật
  _example-cookie-banner:
    status: done
    version: "1.1.0"
    scope: fullstack
    stories_total: 3
    stories_done: 3
    tests_total: 5
    tests_pass: 5
    coverage: 78%                        # 7/9 AC covered

  # Thêm features thật ở đây. Format:
  # {feature-flag}:
  #   status: draft | approved | in-progress | review | done | archived
  #   version: "1.0.0"
  #   scope: backend | frontend | storefront | fullstack
  #   stories_total: 0
  #   stories_done: 0
  #   tests_total: 0
  #   tests_pass: 0
  #   coverage: 0%
